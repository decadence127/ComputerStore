import { Box } from "@mui/system";
import { OrderData } from "../../../redux/services/orderService";

interface OrderDayStatsProps {
  orders: OrderData[];
}

export default function OrderDayStats({ orders }: OrderDayStatsProps) {
  return <Box></Box>;
}
